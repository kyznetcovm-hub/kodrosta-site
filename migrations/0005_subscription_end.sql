-- Дата окончания абонемента резидента, для еженедельных напоминаний
-- об истечении (см. src/subscriptions.js). Заполняется/обновляется при
-- синхронизации с гугл-таблицей «Вступившие» (src/sheets-sync.js) —
-- берётся максимум из столбцов «Дата окончания» на вкладке (после
-- продления там появляется вторая, более поздняя дата).

ALTER TABLE residents ADD COLUMN subscription_end TEXT;
